/**
 * Shared DialogContent shell for client detail + create/edit modals.
 * Keep width/height and flex scroll behavior in sync across all three.
 */
export const CLIENT_MODAL_CONTENT_CLASS =
  'w-full max-w-[calc(100%-2rem)] sm:max-w-3xl lg:max-w-5xl max-h-[90vh] flex flex-col overflow-hidden p-0 gap-0';
