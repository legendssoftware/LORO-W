/**
 * Shared page shell layout classes for staff/app content pages.
 * Matches Competitor Overview (visualiser) width and padding for a wider content area.
 */
export const appPageScrollWrapClass = 'h-full overflow-auto';

/** Primary main content shell: container + max-w-8xl (96.8rem) + page padding. */
export const appPageMainClass =
  'container mx-auto max-w-8xl px-3 py-5 sm:px-6 sm:py-8';
